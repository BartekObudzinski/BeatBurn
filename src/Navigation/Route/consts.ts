import { StackParamList } from '@Navigation/Route/types';
import { createNativeStackNavigator } from '@react-navigation/native-stack';

export const Stack = createNativeStackNavigator<StackParamList>();
